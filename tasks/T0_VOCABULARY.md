"# T0: Implement Symbol Vocabulary (Complete)\n\n> ✅ Status: Completed\n\n## Description\nThe vocabulary maps each Unicode glyph in PAL/Ø to a unique token ID, opcode enum, and arity. It is the foundational lookup table used by the tokenizer and parser.\n\nThis task was completed as part of the initial architecture phase:\n- 60+ glyphs from [GLYPH_SPEC.md](../GLYPH_SPEC.md) are registered in `src/tokenizer/vocabulary.py`\n- Bidirectional mapping: glyph ↔ token_id ↔ opcode_enum\n- Fixed arity (0,1,2,3) encoded per symbol\n- Reserved tokens (`PAD`, `START`, `END`, `UNKNOWN`) defined\n\n## Implementation Details\n- File: `src/tokenizer/vocabulary.py`\n- Function: `init_symbols()`\n- Test coverage: Verified via `examples/demo.py`\n\n## Dependencies\n- Required by: Tokenizer (T1), Parser (T2)\n\n## Validation\n✅ All glyphs in GLYPH_SPEC.md match vocabulary entries exactly.\n✅ Example input `"λλ+₁₀"` correctly tokenizes to `[LAMBDA, LAMBDA, ADD, VAR1, VAR0]`.\n\n## Notes\n- Must remain synchronized with `GLYPH_SPEC.md`. Any change to the spec requires a matching update here.\n- No further development needed. This is the single source of truth for glyph-to-token mapping.\n\n> **Note**: The vocabulary implementation is complete and stable. Future work focuses on using it correctly in downstream components."